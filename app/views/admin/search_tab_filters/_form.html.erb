<%= form_for(@admin_search_tab_filter) do |f| %>
  <% if @admin_search_tab_filter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_search_tab_filter.errors.count, "error") %> prohibited this admin_search_tab_filter from being saved:</h2>

      <ul>
      <% @admin_search_tab_filter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
